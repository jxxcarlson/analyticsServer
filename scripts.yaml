scripts:

  pg-start: pg_ctl -D /usr/local/var/postgres start

  pg-edit: vim /usr/local/var/postgres/postgresql.conf

  pg-stop: pg_ctl -D /usr/local/var/postgres stop

  cloc: cloc --by-file src app

  hello: curl  http://localhost:8080/analytics/hello

